BIGDATA_DOCKER_DATA=D:/docker/bigdata/data

# Docker build proxy.
# Leave these empty when apt-get reports 502 via the local proxy.
# If direct network fails, set them back to http://host.docker.internal:7897.
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=localhost,127.0.0.1,::1,host.docker.internal,mysql,redis,namenode,datanode,resourcemanager,nodemanager,hive-metastore,hiveserver2,spark-master,spark-worker,python

MYSQL_ROOT_PASSWORD=root123456
MYSQL_PORT=13306
HIVE_DB_USER=hive
HIVE_DB_PASSWORD=hive123456

REDIS_PORT=16379

SPARK_WORKER_CORES=2
SPARK_WORKER_MEMORY=2g
